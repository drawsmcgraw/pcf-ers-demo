apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: attendees
  description: Sample tanzu application, pulled from TAS days.
  tags:
    - java
    - spring
    - web
    - tanzu
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=attendees'
spec:
  type: service
  lifecycle: experimental
  owner: default-team
